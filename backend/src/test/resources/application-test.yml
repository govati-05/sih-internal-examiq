spring:
  datasource:
    url: jdbc:h2:mem:examiq-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: always
app:
  jwt:
    secret: ExamiqTestJWTSecretForRegistration2026!
    expiration-ms: 3600000
  ai:
    service-url: http://localhost:8001
  storage:
    path: ./storage-test
